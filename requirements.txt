# Amé Python backend — Linux deps.
#
# System packages you may also need (apt names — equivalents exist on other
# distros):
#   libportaudio2          for sounddevice
#   python3-xlib           for pyautogui (X11 only)
#   xdotool / wmctrl       optional, for advanced window control
#   xdg-utils              for xdg-open (open_file / open_folder / lock_screen)
#   pulseaudio-utils       for `pactl` (set_volume / mute_volume)
#   xclip OR wl-clipboard  for pyperclip
#   mss + grim + scrot     fallback screenshot tools

# Web stack
fastapi>=0.115
uvicorn[standard]>=0.30
python-socketio>=5.11
pydantic>=2.7

# Env / config
python-dotenv>=1.0

# Audio capture (PortAudio backend)
sounddevice>=0.5

# System probing / process control
psutil>=6.0

# Input automation (X11)
pyautogui>=0.9.54
pyperclip>=1.9.0

# Imaging
Pillow>=10.4
mss>=10.0

# HTTP
requests>=2.32
httpx>=0.27

# File watching
watchdog>=5.0

# Web automation (headless browser agent)
playwright>=1.47

# Google / Gemini
google-genai>=0.3.0
google-api-python-client>=2.146
google-auth-oauthlib>=1.2

# Spotify
spotipy>=2.24

# Crypto (Fernet / PBKDF2-HMAC for at-rest memory encryption)
cryptography>=43.0

# File reading (read_file tool — PDF/Excel/Word)
pypdf>=4.0
openpyxl>=3.1
python-docx>=1.1

# Dev / test
pytest>=8.3
